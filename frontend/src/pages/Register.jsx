import{useState}from'react';import{useNavigate}from'react-router-dom';import api from'../api';export default function Register(){const nav=useNavigate();const[f,setF]=useState({name:'',email:'',password:''});const submit=async e=>{e.preventDefault();try{await api.post('/register',f);alert('Registration successful');nav('/login')}catch(x){alert(x.response?.data?.message||'Registration failed')}};return <main className="page"><form className="form" onSubmit={submit}><h2>Register</h2>{['name','email','password'].map(k=><input key={k} type={k==='password'?'password':'text'} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/>)}<button className="btn">Create Account</button></form></main>}
