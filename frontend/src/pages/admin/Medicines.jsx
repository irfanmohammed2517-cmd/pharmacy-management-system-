import{useEffect,useState}from'react';import api from'../../api';const empty={name:'',brand:'',category:'',description:'',price:'',discount:0,stock:'',batchNumber:'',manufacturingDate:'',expiryDate:'',prescriptionRequired:false,image:''};export default function Medicines(){const[data,setData]=useState([]),[f,setF]=useState(empty),[edit,setEdit]=useState(null);const load=()=>api.get('/medicines').then(r=>setData(r.data));useEffect(()=>{ load(); },[]);const submit=async e=>{e.preventDefault();const body={...f,price:Number(f.price),stock:Number(f.stock),discount:Number(f.discount)};if(edit)await api.put(`/medicines/${edit}`,body);else await api.post('/medicines',body);setF(empty);setEdit(null);load()};const del=async id=>{if(confirm('Delete medicine?')){await api.delete(`/medicines/${id}`);load()}};const start=m=>{setEdit(m._id);setF({...m,manufacturingDate:m.manufacturingDate?.slice(0,10)||'',expiryDate:m.expiryDate?.slice(0,10)||''})};return <main className="page"><h2>Manage Medicines</h2><form className="form wide" onSubmit={submit}><input placeholder="Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/><input placeholder="Brand" value={f.brand} onChange={e=>setF({...f,brand:e.target.value})} required/><input placeholder="Category" value={f.category} onChange={e=>setF({...f,category:e.target.value})} required/><input placeholder="Price" type="number" value={f.price} onChange={e=>setF({...f,price:e.target.value})} required/><input placeholder="Stock" type="number" value={f.stock} onChange={e=>setF({...f,stock:e.target.value})} required/><input placeholder="Batch number" value={f.batchNumber} onChange={e=>setF({...f,batchNumber:e.target.value})} required/><input type="date" value={f.manufacturingDate} onChange={e=>setF({...f,manufacturingDate:e.target.value})}/><input type="date" value={f.expiryDate} onChange={e=>setF({...f,expiryDate:e.target.value})}/><textarea placeholder="Description" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><label><input type="checkbox" checked={f.prescriptionRequired} onChange={e=>setF({...f,prescriptionRequired:e.target.checked})}/> Prescription required</label><button className="btn">{edit?'Update':'Add'} Medicine</button>{edit&&<button type="button" onClick={()=>{setEdit(null);setF(empty)}}>Cancel</button>}</form><div className="tablewrap"><table><thead><tr><th>Name</th><th>Category</th><th>Price</th><th>Stock</th><th>Actions</th></tr></thead><tbody>{data.map(m=><tr key={m._id}><td>{m.name}</td><td>{m.category}</td><td>₹{m.price}</td><td>{m.stock}</td><td><button onClick={()=>start(m)}>Edit</button> <button onClick={()=>del(m._id)}>Delete</button></td></tr>)}</tbody></table></div></main>}
