import{useEffect,useState}from'react';import api from'../../api';export default function Users(){const[d,setD]=useState([]);const load=()=>api.get('/admin/users').then(r=>setD(r.data));useEffect(()=>{ load(); },[]);const del=async id=>{if(confirm('Delete user?')){await api.delete(`/admin/users/${id}`);load()}};return <main className="page"><h2>Users</h2><div className="tablewrap"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Action</th></tr></thead><tbody>{d.map(u=><tr key={u._id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.role!=='admin'&&<button onClick={()=>del(u._id)}>Delete</button>}</td></tr>)}</tbody></table></div></main>}
