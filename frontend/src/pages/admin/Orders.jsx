import{useEffect,useState}from'react';import api from'../../api';const statuses=['Pending','Confirmed','Processing','Shipped','Delivered','Cancelled'];export default function Orders(){const[d,setD]=useState([]);const load=()=>api.get('/admin/orders').then(r=>setD(r.data));useEffect(()=>{ load(); },[]);const change=async(id,status)=>{await api.put(`/orders/${id}/status`,{status});load()};return <main className="page"><h2>Orders</h2>{d.map(o=><div className="card" key={o._id}><h3>#{o._id.slice(-6)} — {o.userId?.name||'User'}</h3><p>{o.userId?.email} · ₹{o.totalAmount}</p><p>{o.address}</p><ul>{o.medicines.map(m=><li key={m.medicineId}>{m.name} × {m.quantity}</li>)}</ul><select value={o.status} onChange={e=>change(o._id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></div>)}</main>}
