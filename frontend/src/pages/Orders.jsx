import{useEffect,useState}from'react';import api from'../api';export default function Orders(){const u=JSON.parse(localStorage.getItem('user')||'null');const[data,setData]=useState([]);useEffect(()=>{if(u)api.get(`/orders/user/${u.id}`).then(r=>setData(r.data))},[]);return <main className="page"><h2>My Orders</h2>{data.map(o=><div className="card" key={o._id}><h3>Order #{o._id.slice(-6)}</h3><p>Status: <b>{o.status}</b></p><p>Total: ₹{o.totalAmount}</p><p>{o.address}</p><ul>{o.medicines.map(m=><li key={m.medicineId}>{m.name} × {m.quantity}</li>)}</ul></div>)}</main>}
