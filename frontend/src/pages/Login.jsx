import{useState}from'react';import{useNavigate}from'react-router-dom';import api from'../api';export default function Login(){const nav=useNavigate();const[f,setF]=useState({email:'',password:''});const submit=async e=>{e.preventDefault();try{const r=await api.post('/login',f);localStorage.setItem('user',JSON.stringify(r.data.user));nav(r.data.user.role==='admin'?'/admin':'/medicines')}catch(x){alert(x.response?.data?.message||'Login failed')}};return <main className="page"><form className="form" onSubmit={submit}><h2>Login</h2><input type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})} required/><input type="password" placeholder="Password" value={f.password} onChange={e=>setF({...f,password:e.target.value})} required/><button className="btn">Login</button></form></main>}
